thresholds:
  # 1 -> 2 version
  theta_track: 0.5
  
  # 2 -> 1 or 3 version
  theta_high: 0.7
  theta_low: 0.3

sort:
  iou_threshold: 0.5   # マッチングのIoU閾値
  max_age:       1     # 検出なしで生存させるフレーム数
  min_hits:      1     # 確立済みトラックとみなす最低連続マッチ数

dataset: "KITTI"
iou_threshold: 0.5
integrate_way: "affirmative"

model_1: "yolov8n"
model_2: "rtdetr"
model_3: "fastrcnn"
max_version: 3

